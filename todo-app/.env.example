# Anthropic API key for the conversational interface
# Get yours at https://console.anthropic.com/account/keys
ANTHROPIC_API_KEY=sk-ant-api03-...

# Database URL (for production deployment)
# For Vercel: Use PostgreSQL (e.g., Vercel Postgres, Neon, Supabase)
# For Railway: Can use PostgreSQL or SQLite with persistent volume
DATABASE_URL=postgres://user:pass@host:port/database

# Optional: Database provider (defaults to 'sqlite' for local development)
# Set to 'postgres' for production
# DATABASE_PROVIDER=postgres